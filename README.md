Sources for the plugin `plugin.applovinMax.Pangle`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.applovinMax.Pangle" = {
            publisherId = "com.solar2d",
        },
    },
}
```
